# STEP14 — 실사용 발견 기록 & 대응 (REAL USAGE DISCOVERY & RESPONSE)

> 개설 2026-09-04 / 운영 방식 확정 2026-09-05(CPO 작업지시).
> **새 기능을 만드는 스프린트가 아니다.** 실제 사용 중 발생한 문제를
> `발견 → 재현 → 분류 → 최소 수정 → 재검증`으로 처리한다.
>
> 지금 이 프로젝트의 가장 중요한 질문은 하나다.
> **사장님이 주문:한장을 쓰다가 어느 순간 다시 엑셀·카카오·전화로 돌아가는가?**

## 1. 기록 방식 (CEO/CPO — 길게 쓸 필요 없음)

아래 블록을 복사해 §5에 붙인다. 해결책은 적지 않는다(해결책을 적기 시작하면 관찰이 아니라 설계가 된다).

```text
[STEP14-00N]

상황:
행동:
문제:
결과:
기존 방식:
```

## 2. CTO 처리 절차 (기록이 들어오면 즉시)

1. 실제 화면에서 재현
2. 기존 정책 확인
3. **A/B/C/D 분류**
4. 수정 가능 여부 판단
5. 수정
6. 회귀 게이트 실행([REGRESSION-GATE](../qa/STEP13-PHASE-B/REGRESSION-GATE.md) §1 해당 영역)
7. baseline cleanup 확인(diff = 0)

중간 Gate마다 보고하지 않는다. `발견 → 원인 → 수정 → 재검증 → 다음 발견`으로 이어가고 §6 형식으로만 최종 보고한다.

## 3. 분류 기준 (CPO 라벨, 2026-09-05 갱신)

| 분류 | 뜻 | 예 | CTO 권한 |
|---|---|---|---|
| **A 버그** | 의도된 기능이 정상 동작하지 않음 | 저장 후 값이 사라짐, 필터 결과가 조건과 다름, 다른 테넌트 데이터 노출 | **자율 수정** |
| **B UX / 정책 범위 내 개선** | 기능은 정상인데 멈추거나, 못 믿거나, 용어를 모름 | 다음 단계가 안 보임, 저장됐는지 확신 없음, 용어 이해 못 함 | **제품 정책을 바꾸지 않는 범위에서 최소 수정** |
| **C 기존 도구 복귀 원인** | 제품 안에서 하다가 엑셀·카카오·전화로 나감 | 기사 배정 후 카카오로 다시 전달, 결과 확인하러 엑셀 열기 | **가장 중요.** 원인을 분해해 A·B·D 중 하나로 귀결시킨다 |
| **D 신규 기능 / 정책 판단 필요** | 현재 구조로 해결 불가하거나 기존 동작·기본값·권한·보존 방식이 바뀜 | 카카오 자동 연동, 재고, 자동 처리 여부, 권한 범위 변경 | **수정 금지** — `현상 → 재현 → 근거 → 영향 범위 → 선택지`까지만 보고 |

> C는 권한 등급이 아니라 **발견의 성격**이다. "기능이 없어서 불편하다"보다
> **"기능이 있는데도 결국 엑셀을 다시 열었다"** 가 훨씬 중요한 발견이다.

## 4. 기록해야 할 순간 (사소해 보여도 기록)

| | 순간 | 예 |
|---|---|---|
| **1** | **멈춤** | 어디를 눌러야 할지 모름 · 용어를 이해 못 함(동일인 후보·상품행·배송그룹·일괄 적용·변경사항) · 다음 행동을 고민함 · 제품 메뉴 순서와 실제 업무 순서가 다름 |
| **2** | **되돌아감** | 이전 화면으로 다시 감 · 같은 정보를 다시 확인 · 이미 입력한 내용을 다른 곳에서 다시 찾음 |
| **3** | **외부 도구 복귀** | 엑셀 열기 · 카카오 확인 · 전화 확인 · 기사에게 별도 전달 |
| **4** | **불안해서 확인** | "이거 저장된 거 맞나?" "전체 주문이 들어간 건 아닌가?" "취소 건도 같이 들어갔나?" "기사에게 제대로 전달됐나?" |

> **4번은 기능 부족이 아니라 신뢰 UX 문제일 가능성이 크다.** 기능을 추가하기 전에
> "지금 무엇이 반영됐는지"를 보여주는 것으로 해결되는지 먼저 본다.
>
> 3번을 기록할 때는 **직전 행동까지** 함께 남긴다(예: "기사 배정까지 하고 나서 카카오를 열어 다시 전달") — 재현과 원인 분리가 정확해진다.

## 5. 발견 기록

<!-- CEO/CPO가 §1 양식으로 추가. CTO는 각 항목 아래에 [분류]/[재현]/[조치]만 덧붙이고,
     위 기록 원문은 수정하지 않는다. -->

### [STEP14-000] (참고 — 실사용 기록 아님)

```text
상황:   엑셀 주문을 등록하려고 함
행동:   전체 주문 가져오기를 선택
문제:   오늘 주문만 등록해야 하는데 전체 주문이 들어갈까 봐 불안함
결과:   안내를 읽고 진행했지만 확신이 없음
기존 방식: 엑셀에서 날짜 필터를 먼저 확인함
```

- **출처**: CPO 작업지시서(2026-09-05)의 **양식 예시**다. 실제 사장님 발언 기록이 아니므로 번호를 000으로 두고 통계에서 제외한다.
- **분류**: B (기록 순간 4 — 불안해서 확인)
- **조치**: 이미 반영됨 — 사업장별 가져오기 범위 기본값 기억, `(기본)` 표기 제거, 전체 주문 선택 시 인라인 안내, 분석 결과에 적용 범위 표시(`dfd1f71`, QA 26/26 PASS).

## 6. 최종 보고 형식

```text
STEP14 REPORT

발견: N건
A 버그:            N건 — 수정 완료 / QA 결과
B UX·정책 내 개선: N건 — 수정 N / 판단 필요 N
C 기존 도구 복귀:  N건 — 원인 분해 결과(A/B/D 귀결)
D 신규·정책 판단:  N건 — 개발 안 함, CEO/CPO 판단 대기

실사용 중 기존 도구 복귀: 엑셀 N회 / 카카오 N회 / 전화 N회

가장 큰 발견: [한 문장]

다음 우선순위: P1 … / P2 …
```

## 7. 경계

**자율**: QA 노후화 · flaky·상태 오염 · 범위 내 제품 버그 · UX 명확화 · 용어 개선 · 안내 추가 · 기존 흐름 연결 개선 · cleanup · baseline 재측정 · 회귀 실행 · 정적/배포 검증.

**금지**: assertion 삭제 · 대상 축소 · sleep으로 억지 PASS · 무한 retry · **발견되지 않은 신규 기능 선제 구현**.

**BLOCKER 4종**(①제품 정책 ②비가역 데이터 ③권한·보안 ④신규 기능/범위 확대)은 `현상 → 재현 → 근거 → 영향 범위 → 선택지`까지만 정리하고 멈춘다.

**수정 후 최소 QA**: 해당 기능 QA + 영역 필수 회귀 + (영향 시) user3/user6 격리 + cleanup baseline diff = 0 + tsc·ESLint·Build·Production·git clean. 제품 화면 구조가 바뀌면 [Landing Product Parity Gate](../qa/STEP13-PHASE-B/REGRESSION-GATE.md#1-1-landing-product-parity-gate-cpo-결정-2026-09-05)도 적용한다.

## 8. 이 단계에서 만들지 않기로 한 것 (CPO 확정)

카카오 주문 연동 / 자동화 / AI 추천 / 재고관리 / CRM 마케팅 확장 / 경로 최적화.
**실사용에서 어디가 가장 아픈지 확정되기 전에 넣으면 "기능 많은 SaaS"로 되돌아간다.**
