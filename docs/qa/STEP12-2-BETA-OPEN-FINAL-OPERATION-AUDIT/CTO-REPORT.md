# STEP12-2-BETA-OPEN-FINAL-OPERATION-AUDIT — CTO 최종보고

- 감사 대상 배포: `e17ce27` (STEP12-1까지 반영된 Production, 이번 Gate는 앱 코드 변경 없음)
- 대상 URL: `https://jumunhanjang.vercel.app`
- 실행 QA 스크립트: `scripts/qa/step12-2-final-operation-audit.ts` (신규)
- 실행 결과: 로컬 2회(12/12, 12/12) + Production 1회(12/12) — 총 3회 모두 전건 PASS
- 테스트 테넌트: `user3` (QA-safe, `RUN_TAG=QA-1202-<timestamp>`로 완전 격리·자동원복)

---

## ⚠️ CPO 필독 — 성능 실측에서 발견된 구조적 특이사항 (Section H)

CPO가 지시한 "150건 배치 저장 ~33초 기준선 유지, 단순히 느리다는 이유만으로 수정하지 말 것"이라는 전제 자체를 재검토해야 할 근거가 나왔습니다.

**5건짜리 혼합 저장과 150건짜리 단일필드 일괄 저장의 소요 시간이 거의 동일했습니다.**

| 측정 | 로컬 1회차 | 로컬 2회차 | Production |
|---|---|---|---|
| H1: 5건 혼합 저장(개별 기사변경 2 + 가방번호 1 + 기사재변경 1 + 회수토글 1) | 32,246ms | 31,612ms | 33,670ms |
| H2: 150건 단일필드 일괄 저장 | 33,516ms | 31,550ms | 33,076ms |
| (참고) STEP11-13 당시 Production 150건 기준선 | — | — | ~32,955ms |

즉 "150건이라 느리다"가 아니라 **"저장 버튼을 누르는 모든 동작이 배치 크기와 무관하게 ~30초 고정비용을 가진다"**는 뜻입니다. 사장님이 배송건 1건만 고쳐서 저장해도 30초, 150건을 한 번에 고쳐서 저장해도 30초로 체감 시간이 같다는 의미이며, 이는 STEP11-13 최초 보고 시점의 "큰 배치라서 느리다"는 설명과 다른, 더 근본적인 구조적 원인을 시사합니다.

**이번 Gate에서 수행/배제한 것 (CPO 지시 준수):**
- 코드 수정을 하지 않았습니다.
- 후보 원인 2가지를 읽기전용으로 배제만 했습니다:
  - Vercel 콜드스타트 → 배제(로컬 상시구동 Node 프로세스에서도 동일 크기로 재현됨)
  - "오늘" 배송 잔존 데이터량이 많아 `revalidatePath` 재렌더 비용이 큰 경우 → 배제(측정 시점 `user3`의 "오늘" shipment는 1건뿐이었음)
- `assignDriver`/`updateBagBatch`/`compactRouteOrder`/`revalidatePath` 자체, 혹은 Supabase 연결·재시도 백오프 등 더 깊은 원인은 조사하지 않았습니다(코드 수정 없이 사실관계만 보고하라는 지시 범위를 넘어섬).

**CPO 판단이 필요한 이유:** 이 특성이 P0/P1 중단조건(데이터유실/권한우회/저장후소실/핵심업무불가)에는 해당하지 않아 감사를 중단하지 않았지만, "저장" 버튼 클릭 1회의 체감 응답성이 배치 규모와 무관하게 일정하게 느리다는 것은 베타 오픈 후 사장님의 일상 사용 경험에 계속 반복적으로 영향을 줄 수 있는 사안입니다. GO/HOLD 판단, 혹은 별도 성능 전용 Gate 필요 여부를 CPO가 직접 판단해 주시기 바랍니다.

---

## 1. 테스트한 실제 사장님 흐름

- **A. 신규 사장님 첫 로그인(기사0/주문0)**: STEP12-1에서 이미 검증된 안내 배너(`delivery-live-filters.tsx`)를 코드로 재확인. 기사 CRUD는 `driver-management-card.tsx`를 코드 리뷰로 확인 — 등록/수정/비활성화/삭제 전부 이미 존재하며 Admin 전용 기능(테넌트 초기화 등)은 사장님 화면에 노출되지 않음(코드상 `role==='admin'` 가드 확인).
- **B. 주문 10건 이상 등록 → 배송관리 발견성**: QA 스크립트로 그룹 3건 + 개별 2건 seed 후 배송관리 접속 — 상태 탭(배정필요/배송중/완료), 지역 필터, 그룹 카드가 자연스럽게 노출됨을 스크린샷으로 확인. 그룹 기능을 억지로 유도하지 않고 실제 같은 주소 3건이 자동으로 그룹으로 묶인 자연스러운 케이스만 사용.
- **C. Draft 저장 UX — 5종 혼합 편집**: A(그룹행 기사선택) / B(그룹행 기사선택) / C(가방번호 입력) / D(개별행 기사 재변경, 그룹적용값 override) / E(개별행 회수여부 토글)를 한 세션에서 혼합 실행 → 저장 전 상태(사이드바 안내문 + 상단 토스트 + "변경사항 저장"/"전체 되돌리기" 버튼) 스크린샷 확보([`screenshots/step12-2-c1-mixed-draft.png`](screenshots/step12-2-c1-mixed-draft.png)) → 저장 → 새로고침 후 DB 값 유지 확인.
- **D. 개별입력 vs 체크박스/그룹일괄배정 시각 분리**: 기본상태(0건 선택, 일괄배정 바 없음) → 그룹 체크(3건, 초록 바 등장) → 개별 2건 추가 체크(5건, 각 행 초록 테두리) → 일괄 적용 4단계를 스크린샷으로 확보. 원본 파일: [`screenshots/step12-2-d1-default.png`](screenshots/step12-2-d1-default.png), [`screenshots/step12-2-d2-group-selected.png`](screenshots/step12-2-d2-group-selected.png), [`screenshots/step12-2-d3-mixed-selection.png`](screenshots/step12-2-d3-mixed-selection.png), [`screenshots/step12-2-d4-after-bulk-apply.png`](screenshots/step12-2-d4-after-bulk-apply.png).
- **G. 신규사장님→기사등록→주문등록→배송관리→개별기사선택→가방번호입력→변경사항저장→체크박스선택→일괄기사적용→변경사항저장→새로고침→값유지**: 전체 체인을 스크립트 한 세션에서 순서대로 실행, DB 검증 포함 12/12 PASS.

## 2. 발견한 문제

| # | 내용 | 분류 |
|---|---|---|
| 1 | Section H: 저장 소요시간이 배치 크기와 무관하게 ~30초 고정으로 나타남 (위 "CPO 필독" 참조) | **P1 — CPO 판단 필요** (CPO 정의상 P0/P1 중단조건에는 미해당, 그러나 일상 체감에 지속 영향) |
| 2 | `user3`에 이전 세션(오늘 이전)의 진단 스크립트가 남긴 잔존 주문 1건("QA-DIAG-수령")·기사 1명("QA-DIAG-기사")이 배송관리 화면에 노출되어 있었음 | P2 — 이번 Gate 중 발견 즉시 삭제 완료(아래 7번 참조), 이번 Gate가 만든 데이터는 아니었음 |
| 3 | `user3`에 그 밖에도 오래된 QA 프리픽스 기사 2명("QA-테스트기사A-1/2")과 QA 프리픽스 고객 6건, `delivery_groups` 잔여 1건이 존재 | P2 — STEP12-1 때부터 이미 알려진 비차단성 잔존물, 이번 Gate 범위 밖(별도 정리 필요 시 CPO 지시로 진행) |

Section A/B/C/D/E/F/G 자체에서는 위 표의 항목 외에 UX 혼동, 데이터 분리 실패, 권한 우회, Draft 소실 등 **기능적 결함은 발견되지 않았습니다.**

## 3. P0/P1/P2 분류

- **P0(즉시중단 대상): 없음.** 데이터 유실, 타 사장님 데이터 노출, "저장했는데 값이 사라짐", 권한 우회 — 전부 미발생 확인.
- **P1(CPO 판단 필요): 항목 1건** — 저장 소요시간의 배치-크기-무관 고정비용 특성(위 "CPO 필독" 참조). CPO가 정의한 P1 중단조건(신규사장님 핵심업무 진행 불가/기사·주문·배송 핵심흐름 막힘/Draft저장이 오히려 혼란)에는 해당하지 않아 감사를 중단하지 않고 끝까지 진행했으나, 별도로 명시 보고합니다.
- **P2(하지 않음): 2건** — 오래된 QA 잔존 데이터(위 표 2, 3번). 2번은 이번 Gate 중 즉시 삭제, 3번은 범위 밖으로 남겨둠.

## 4. 수정 내용

**이번 Gate에서는 앱 코드를 수정하지 않았습니다.** CPO의 "코드를 먼저 수정하지 말 것 / 발견한 문제만 수정" 원칙에 따라, 발견된 P1(성능)은 원인 불명확 상태에서 임의 수정하지 않았고, P0/P1(CPO 정의 기준)에 해당하는 기능 결함이 없었으므로 수정할 대상 자체가 없었습니다. 유일한 조치는 `user3`에 남아있던 오래된 진단 스크립트 잔존 데이터(주문 1건 + 기사 1명) 삭제였으며, 이는 QA-safe 테넌트의 데이터 정리이지 앱 코드 변경이 아닙니다.

## 5. Production 재검증 결과

- `scripts/qa/step12-2-final-operation-audit.ts`를 로컬 2회, Production(`https://jumunhanjang.vercel.app`) 1회 실행 — **3회 전부 12/12 PASS**.
- 앱 코드 변경이 없으므로 별도 배포/재배포 없이 기존 배포(`e17ce27`)에 대해 그대로 검증했습니다.
- `npx tsc --noEmit`: 통과(에러 없음). 단, 검증 도중 이전 세션에서 중단된 preview 서버가 남긴 손상된 `.next/dev/types/validator.ts` 아티팩트가 최초 실행 시 에러를 냈으나, 이는 앱 코드 결함이 아니라 dev 서버 산출물 문제였고 `.next/dev/types` 삭제 후 재실행하여 정상 통과를 확인했습니다.
- `npx eslint scripts/qa/step12-2-final-operation-audit.ts`: 통과(에러 없음).

## 6. 성능 실측

위 "CPO 필독" 표 참조. 요약:
- H1(5건 혼합 저장): 로컬 32.2s/31.6s, Production 33.7s
- H2(150건 일괄 저장): 로컬 33.5s/31.6s, Production 33.1s
- STEP11-13 원 기준선(Production 150건): ~33.0s
- **결론: 배치 크기(5건 vs 150건)가 소요시간에 유의미한 영향을 주지 않음** — STEP11-13 시점의 "큰 배치라 느리다"는 설명과 다른, 고정비용 성격의 지연으로 재해석 필요.

## 7. QA 데이터 삭제 확인

- `step12-2-final-operation-audit.ts`가 생성한 모든 데이터(`QA-1202-` 프리픽스 주문/배송건/고객/그룹, `driverA`/`driverB`/`perfDriver` 기사 3명과 로그인 계정)는 스크립트 자체 `finally` 블록에서 자동 원복되며, 3회 실행 후 아래 read-only 쿼리로 재확인했습니다.

```
remaining QA orders on user3 (QA-1202-*): []
remaining delivery_groups from this run: 0 (this run 생성분 기준)
```

- 이번 Gate 도중 발견한 **이전 세션의 잔존물**("QA-DIAG-수령" 주문 1건, "QA-DIAG-기사" 기사 1명)은 이번 Gate가 만든 것이 아니지만 직접 삭제 완료했습니다.
- 이 Gate 범위 밖의 **더 오래된 잔존물**(QA-테스트기사A-1/2 기사 2명, QA-프리픽스 고객 6건, delivery_groups 1건)은 STEP12-1 시점부터 이미 알려진 비차단성 항목으로 남겨두었습니다(실제 `user1` 등 진짜 사장님 데이터에는 영향 없음, `user3`는 QA-safe 테넌트).

## 8. 최종판정

- Section A~G 전 항목에서 P0 없음, CPO 정의 기준 P1(핵심업무 진행불가/핵심흐름 막힘/Draft저장 혼란) 없음.
- Section D(개별입력 vs 체크박스/그룹일괄배정 시각 분리)는 스크린샷 4장으로 직접 확인 — 기본상태에 일괄배정 바가 전혀 노출되지 않고, 체크 시에만 명확한 초록색 바 + "적용은 화면에만 반영됩니다" 안내가 등장하며, 개별 체크와 그룹 체크가 시각적으로 충돌 없이 공존함을 확인.
- Section H에서 배치-크기-무관 고정비용 성격의 저장 지연(~30~34초)이라는, STEP11-13 당시 프레이밍과 다른 새로운 구조적 사실이 발견되어 위에 프로미넌트하게 보고합니다. 이는 CPO가 정의한 중단조건에는 해당하지 않으므로 감사를 통과시키되, CPO의 GO/P1수정후GO/HOLD 판단에 반드시 참고되어야 할 사항입니다.

**CTO FINAL: PASS**
